repos:
- repo: local
  hooks:
  - id: black
    name: black
    entry: uv run black .
    language: system
    pass_filenames: false

- repo: local
  hooks:
  - id: ruff
    name: ruff
    entry: uv run ruff --fix --exit-non-zero-on-fix .
    language: system
    pass_filenames: false
